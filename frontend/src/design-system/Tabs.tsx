// Pure re-export of the real, CLI-generated primitive — see
// components/ui/tabs.tsx. Nothing to adapt, so nothing is duplicated.
export { Tabs, TabsContent, TabsList, TabsTrigger } from '@/components/ui/tabs';
